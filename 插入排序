void InsertSort(int *arr, int len)
{
int i = 0;
int j = 0;
int tmp = 0;

for(i = 1; i < len; i++)
{
   tmp = arr[i];
   for(j = i - 1; j >= 0; j--)
  {
      if (tmp > arr[j])
     {
         break;
     }
     arr[j + 1] = arr[j];
  }
  arr[j + 1] = tmp;
 }
}
